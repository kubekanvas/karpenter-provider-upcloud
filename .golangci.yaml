version: "2"
run:
  tests: true
  timeout: 5m
  issues-exit-code: 1
output:
  formats:
    text:
      path: stdout
linters:
  enable:
    - asasalint
    - asciicheck
    - bidichk
    - bodyclose
    - containedctx
    - contextcheck
    - copyloopvar
    - cyclop
    - decorder
    - dogsled
    - dupword
    - durationcheck
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - gocheckcompilerdirectives
    - gochecksumtype
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - goheader
    - goprintffuncname
    - gosec
    - loggercheck
    - makezero
    - misspell
    - nestif
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    - nolintlint
    - predeclared
    - reassign
    - recvcheck
    - revive
    - staticcheck
    - testifylint
    - thelper
    - unconvert
    - unparam
    - usestdlibvars
    - usetesting
    - whitespace
    - govet
    - ineffassign
    - unused
  settings:
    cyclop:
      max-complexity: 15
    exhaustive:
      # Switches over UpCloud's string-typed states and error codes intentionally handle only the
      # cases this provider reacts to.
      default-signifies-exhaustive: true
    goconst:
      min-len: 3
      min-occurrences: 5
    gocritic:
      enabled-tags:
        - diagnostic
        - experimental
        - opinionated
        - performance
        - style
      settings:
        captLocal:
          paramsOnly: true
        rangeValCopy:
          sizeThreshold: 256
    goheader:
      template: |-
        Licensed under the Apache License, Version 2.0 (the "License");
        you may not use this file except in compliance with the License.
        You may obtain a copy of the License at

            http://www.apache.org/licenses/LICENSE-2.0

        Unless required by applicable law or agreed to in writing, software
        distributed under the License is distributed on an "AS IS" BASIS,
        WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
        See the License for the specific language governing permissions and
        limitations under the License.
    misspell:
      locale: US
    nolintlint:
      require-explanation: true
      require-specific: true
    revive:
      rules:
        - name: dot-imports
          disabled: true
    unparam:
      check-exported: false
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - goheader
        path: zz_(.+)\.go
      - path: (.+)\.go$
        text: declaration of "(err|ctx)" shadows declaration at
      # operatorpkg's status.NewController takes the legacy client-go record.EventRecorder.
      # controller-runtime exposes that type only through the deprecated GetEventRecorderFor;
      # GetEventRecorder returns the new events.EventRecorder, which does not typecheck against it.
      # Until operatorpkg adopts the new events API there is no non-deprecated way to build this.
      #
      # The exclusion lives here rather than as an inline //nolint because `golangci-lint run --fix`
      # strips the directive as unused, which silently reintroduces this failure in CI.
      - linters:
          - staticcheck
        path: pkg/controllers/controllers\.go
        text: "SA1019: mgr.GetEventRecorderFor is deprecated"
    paths:
      - hack
      - charts
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gci
    - gofmt
    - goimports
  settings:
    gci:
      sections:
        - standard
        - default
        - blank
        - dot
        - prefix(github.com/kubekanvas/karpenter-provider-upcloud)
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/kubekanvas/karpenter-provider-upcloud
  exclusions:
    generated: lax
    paths:
      - zz_(.+)\.go
      - hack
      - charts
      - third_party$
      - builtin$
      - examples$
